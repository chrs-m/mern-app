{
    "name": "yrgo-mern-app",
    "version": "1.0.0",
    "engines": {
        "node": "16.x"
    },
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "lerna run --parallel dev",
        "start": "lerna run --parallel start"
    },
    "private": true,
    "devDependencies": {
        "lerna": "^4.0.0"
    }
}
